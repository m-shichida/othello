import styled from 'styled-components'

const Container = styled.article`
  display: flex;
  flex-direction: row;
`

const BoardContainer = styled.section`

`

const GameInfo = styled.section`
  margin-left: 20px;
`

export default {
  Container, BoardContainer, GameInfo
};
